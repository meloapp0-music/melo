# v1.0.3 — Venue Links

- Started: 2026-05-06
- Status: in-progress (code shipped to main; awaiting v1.0.2 App Review
  clearance before submission)
- Last updated: 2026-05-06

## Context

First minor-feature release after v1.0.2. Bundles Phase 1 of the
venue-and-merch-links initiative — official venue page link on every
ShowDetail. Per the staged-release plan adopted 2026-05-06, v1.0.3
ships small (one feature) so any regression is unambiguous.

Held back intentionally:
- Email MFA → v1.0.4 (security-sensitive, deserves its own release
  signal for support/diagnostics)
- Buddies Phase 2, recommendations, Wrapped map, contacts, etc. →
  v1.1+

## What's in v1.0.3

- **Venue page link on every show.** Tap "Venue page" pill on
  ShowDetail → opens the official venue site in Safari. For shows
  logged before this release, a "Find venue page" button does an
  on-demand Ticketmaster lookup and saves the result.
- Migration `0006_venue_url.sql` adds the `venue_url` column.
- Mapping in `db/shows.js`, capture in `LogShow` autofill, render +
  resolver in `ShowDetail`, frosted-pill styles in `App.css`.

Full implementation per `2026-05-05-venue-and-merch-links.md`.

## Pre-flight checklist

Run before bumping the version in Xcode:

- [ ] **Run `0006_venue_url.sql`** in Supabase Dashboard → SQL Editor.
  One-time. Idempotent (`add column if not exists`), so safe to run
  even if it was previously applied. Verify with:
  `select column_name from information_schema.columns where
   table_name = 'shows' and column_name = 'venue_url';`
- [ ] Confirm v1.0.2 (Build 6) status in App Store Connect — must be
  "Ready for Sale" before v1.0.3 submission. If still "In Review,"
  hold.
- [ ] `npm run build` clean (already verified 2026-05-06)
- [ ] Test in dev: log a new show, pick a Ticketmaster autofill
  result, verify "Venue page" pill renders as a live link on
  ShowDetail.
- [ ] Test on existing show: open ShowDetail for a pre-v1.0.3 show,
  tap "Find venue page," confirm it resolves and the next open
  shows the live link without the lookup button.

## Ship steps (when v1.0.2 clears)

1. In Xcode, bump:
   - `MARKETING_VERSION` → `1.0.3`
   - `CURRENT_PROJECT_VERSION` → `7`
2. `npm run build && npx cap sync ios`
3. Open `ios/App/App.xcworkspace`, select "Any iOS Device," Product →
   Archive
4. Distribute App → App Store Connect → Upload
5. In App Store Connect, create the v1.0.3 version, attach Build 7,
   paste release notes (below), submit for review

## App Store release notes (draft)

```
Tap into the venue.

Every show now has a "Venue page" link that takes you straight to
the official site — perfect for checking the layout before a show,
finding parking, or browsing the upcoming calendar.

For shows you've already logged, just tap "Find venue page" and we'll
look it up.

More to come.
```

## Changes made

- 2026-05-06: All code merged to `main` (commit `1e003b4`).
  Migration written but not yet applied to Supabase. Awaiting v1.0.2
  App Review clearance before bumping version + archiving.

## Open questions / follow-ups

- **Marketing**: this is the first feature release worth a real
  push. Mojo Reel possibility — "Tap any venue" 6-second demo. Track
  in marketing/Reels follow-ups, not here.
- **Watch the Ticketmaster venue match rate** in production once
  shows start saving with a `venue_url`. If <50% of new logs land a
  URL, the strict-match logic in `lookupVenueUrl` is too tight and
  needs loosening (or a Setlist.fm fallback).
- **Empty-state UX**: the "Venue page not found" pill is a dead-end.
  Future polish: surface a Google search link as a last resort.
